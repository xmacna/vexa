# Integração do fork XMACNA — 19/09/2026

A consolidação preserva o runtime de `c3c44696`, que contém chat confirmado e a receita final da imagem `0.2.2-c3c44696`. O merge reconcilia a main `e0b356d6` incluindo seus quatro arquivos exclusivos de release (notas, witness e os gates value/witness); o witness v0.12.22 permanece histórico, sem alegação de nova execução produtiva.

Os workflows que escrevem no namespace Docker Hub `vexaai/*` ficam limitados ao repositório `Vexa-ai/vexa`. Atestações e validações de releases do próprio fork mantêm seu escopo local. Releases corporativos continuam pelo comando `xmacna meetings release-vexa`, com direitos declarados, imagem imutável, manifest e readback. A main não dispara promoção automática de produção.

A captura de sinais continua preservada: `inv.captureSignalEnabled` controla o gravador, com fallback `VEXA_CAPTURE_SIGNAL=1` no caminho local; `VEXA_CAPTURE_SIGNAL_MAX_BYTES` limita a fita, com padrão de 250 MiB e fallback seguro para valor inválido. Consolidação de histórico não removeu esse código. As notas upstream de v0.12.22 permanecem como recebidas da main.

Readback atual do ECR confirmou `public.ecr.aws/p1v1v3r3/vexa-meetings:0.2.2-c3c44696` no digest `sha256:74fdca00efad8fb922a8e6f7569aeae550e0939081001182c10a9dcacb64f7b6`. O host hoje acessível pelo alias local do Marvin executa outro stack e tem `meetings-edge` inativo; a evidência de agosto não prova instalação atual. Não houve redeploy. A issue Elysium #524, o canário real de Meet e a instalação Nox continuam fora do aceite desta integração.

A revisão independente cobriu fluxo de publicação, preservação de runtime e coerência da evidência. Nenhum publisher alcança upstream no fork. A ressalva factual sobre captura está esclarecida acima; os gates completos passaram em 20/09/2026 (237,68 s, pico RSS 610 MiB aproximado), com Python em 12 pacotes, contratos, build/test Node, replay, telemetria e Compose real. O instrumento oficial MOCK_BOT=1 exercitou o backend sem alegar admissão em Meet. As imagens de serviço foram construídas desta árvore e reutilizadas sem rebuild na prova final; stress e chaos mantêm seus skips opt-in previstos. O autor escolheu explicitamente a via Independent do DCO antes do commit, e a identidade local foi conferida. Backups privados incluem bundles, refs/reflogs e arquivos locais com hashes e restauração verificada. O Dockerfile sujo do worktree image-proof foi preservado no backup: é anterior à receita final e tem paths Python já corrigidos em c3c44696; não deve sobrepor a receita operacional.

O gate Compose detectou que os caminhos Docker Hub `minio/minio` e `minio/mc` deixaram de resolver. Compose e Helm agora usam os mesmos componentes do fornecedor oficial em Quay, com releases explícitas e digest multiarch; o manifesto de licenças mantém a exceção de sidecar já aprovada. Isso restaura pulls novos, sem trocar storage nem executar migração. O init do Compose agora propaga erros com `set -e`, como o Helm já fazia; um controle negativo com o cliente real comprovou que credenciais/endpoint inválidos não resultam em falso sucesso. Não é atualização de segurança do MinIO nem altera uma instalação já existente. Fonte do fornecedor: https://github.com/minio/minio/blob/master/docs/docker/README.md .

A correção MinIO recebeu revisão independente adicional: fornecedor, digests multiarch, composição do image ref no Helm e continuidade da exceção de licença foram conferidos. Helm lint/template e os 19 testes do gate de arquitetura/licenças passaram. Uma primeira gravação retornou 500 sem log preservado pelo harness; a repetição diagnóstica e a suíte final confirmaram gravação bem-sucedida. Não se atribuiu uma causa não demonstrada. Logs de falha passaram a ser coletados por plugin privado antes do teardown, sem alterar o resultado dos testes.

A CI remota fresca revelou dois cenários de polling do teste de chat sensíveis ao escalonamento (contagem exata de polls e janela de priming com sleeps de poucos milissegundos). Os cenários equivalentes agora usam relógio controlado do `node:test`, com a mesma ordem e assertivas; o relógio é restaurado em `finally`. A suíte de chat passou em Node 22 real, com a árvore montada somente para leitura e sem rede, além do typecheck. Os cenários de envio/readback/timeout continuam com tempo real. O código de produção permanece idêntico ao marco operacional.
